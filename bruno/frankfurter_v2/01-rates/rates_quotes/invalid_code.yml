info:
  name: invalid_code
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/rates?quotes=XYZ"
  params:
    - name: quotes
      value: XYZ
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  ### Objective
  Verify API behavior when `quotes` contains an unsupported currency code.

  ### Input
  `quotes=XYZ`

  ### Expected
  The request should be rejected with a validation error.

  ### Actual
  HTTP `422`

  `invalid currency: XYZ`

  The unsupported currency is rejected.
