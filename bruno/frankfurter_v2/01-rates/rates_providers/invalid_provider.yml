info:
  name: invalid_provider
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/rates?providers=XYZ&quotes=USD"
  params:
    - name: providers
      value: XYZ
      type: query
    - name: quotes
      value: USD
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  ### Objective
  Verify API behavior when `providers` contains an unsupported provider key.

  ### Input
  `providers=XYZ`

  `quotes=USD`

  ### Expected
  Determine how the API handles an unsupported provider value.

  ### Actual
  HTTP `200`

  ```json
  []
  ```
