info:
  name: empty
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/rates?providers=&quotes=USD"
  params:
    - name: providers
      value: ""
      type: query
    - name: quotes
      value: USD
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  ### Objective
  Verify API behavior when the `providers` parameter is empty.

  ### Input
  `providers=`

  `quotes=USD`

  ### Expected
  Determine how the API handles an empty provider value.

  ### Actual
  HTTP `200`

  ```json
  []
  ```
